// const express = require('express');
// const route = express.Router();
// const RuleController = require('../controllers/RuleController.js');

// route.post('/addRule',RuleController.addRule);
// route.get('/getRule',RuleController.getRule);
// // Make sure to always add a slash both before and after the endpoint!
// module.exports = route